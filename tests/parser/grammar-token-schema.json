{
    "title": "Grammar token schema v1",
    "type": "object",
    "required": ["type", "value"],
    "properties": {
        "type": {
            "type": "string"
        },
        "value": {
            "type": ["null", "boolean", "object", "array", "number", "string"]
        }
    }
}
